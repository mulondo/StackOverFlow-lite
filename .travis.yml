language: python
python:
- "3.5.2"
install:
- pip install -r requirements.txt
- pip install pytest
- pip install pytest-cov
- pip install coveralls


script:
- python -m unittest discover
- pytest --cov app
after_success:
- coveralls
